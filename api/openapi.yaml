openapi: "3.0.0"
info:
  title: Petstore API
  version: "1.0.0"
paths:
  /pets:
    get:
      operationId: listPets
      summary: List all pets
      tags: [pets]
      responses:
        "200": { description: ok }
  /unicorns:
    get:
      operationId: listUnicorns
      summary: GIT-ADDED unicorns endpoint
      tags: [unicorns]
      responses:
        "200": { description: ok }
